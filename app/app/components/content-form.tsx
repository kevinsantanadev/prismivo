"use client";

import { CheckCircle2, FilePlus2, LoaderCircle, X } from "lucide-react";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import { getOperationalCopy } from "@/lib/app-operational-i18n";
import type { SiteLocale } from "@/lib/site-locale";

export function ContentForm({ locale = "pt-BR" }: { locale?: SiteLocale }) {
  const copy = getOperationalCopy(locale);
  const labels = copy.content;
  const router = useRouter(); const [open, setOpen] = useState(false); const [submitting, setSubmitting] = useState(false); const [message, setMessage] = useState(""); const [success, setSuccess] = useState(false);
  async function submit(event: FormEvent<HTMLFormElement>) {
    event.preventDefault(); if (submitting) return; const form = event.currentTarget; const data = new FormData(form); setSubmitting(true); setMessage(""); setSuccess(false);
    try { const response = await fetch("/api/content", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ kind: data.get("kind"), slug: data.get("slug"), title: data.get("title"), excerpt: data.get("excerpt"), body: data.get("body"), tags: String(data.get("tags") ?? "").split(",").map((tag) => tag.trim()).filter(Boolean), status: data.get("status") }) }); const result = await response.json() as { ok: boolean; error?: { message: string } }; if (!response.ok || !result.ok) { setMessage(locale === "pt-BR" && result.error?.message ? result.error.message : labels.form.error); return; } form.reset(); setSuccess(true); setMessage(labels.form.success); router.refresh(); } catch { setMessage(copy.common.connectionError); } finally { setSubmitting(false); }
  }
  return <div className="quick-project"><button className="app-primary-button" type="button" onClick={() => setOpen(true)}><FilePlus2 aria-hidden="true" />{labels.form.newContent}</button>{open && <div className="app-dialog-backdrop" role="presentation" onMouseDown={(event) => { if (event.target === event.currentTarget) setOpen(false); }}><form className="app-dialog content-dialog" onSubmit={submit} aria-labelledby="content-form-title"><div className="project-form-heading"><div><span className="panel-kicker">{labels.form.studio}</span><h2 id="content-form-title">{labels.form.create}</h2></div><button type="button" onClick={() => setOpen(false)} aria-label={labels.form.close}><X aria-hidden="true" /></button></div><div className="content-form-grid"><div className="form-field"><label htmlFor="content-kind">{labels.form.format}</label><select id="content-kind" name="kind" defaultValue="article"><option value="article">{labels.kinds.article}</option><option value="case_study">{labels.kinds.caseStudy}</option><option value="service">{labels.kinds.service}</option><option value="help">{labels.kinds.help}</option></select></div><div className="form-field"><label htmlFor="content-status">{labels.form.initialState}</label><select id="content-status" name="status" defaultValue="draft"><option value="draft">{labels.statuses.draft}</option><option value="published">{labels.statuses.published}</option></select></div></div><div className="form-field"><label htmlFor="content-title">{labels.form.title}</label><input id="content-title" name="title" minLength={4} maxLength={160} required autoFocus /></div><div className="form-field"><label htmlFor="content-slug">{labels.form.slug}</label><input id="content-slug" name="slug" minLength={3} maxLength={100} pattern="[a-z0-9]+(?:-[a-z0-9]+)*" placeholder={labels.form.slugPlaceholder} required /><small>{labels.form.slugHelp}</small></div><div className="form-field"><label htmlFor="content-excerpt">{labels.form.excerpt}</label><textarea id="content-excerpt" name="excerpt" minLength={20} maxLength={320} rows={3} required /></div><div className="form-field"><label htmlFor="content-body">{labels.form.body}</label><textarea id="content-body" name="body" minLength={80} maxLength={30000} rows={9} required /></div><div className="form-field"><label htmlFor="content-tags">{labels.form.tags} <span>({labels.form.tagsHint})</span></label><input id="content-tags" name="tags" maxLength={260} placeholder={labels.form.tagsPlaceholder} /></div>{message && <div className={`form-message ${success ? "success" : ""}`} role="status">{success && <CheckCircle2 aria-hidden="true" />}{message}</div>}<div className="dialog-actions"><button className="button button-secondary" type="button" onClick={() => setOpen(false)}>{copy.common.cancel}</button><button className="app-primary-button" type="submit" disabled={submitting}>{submitting ? <><LoaderCircle className="spin" aria-hidden="true" />{copy.common.saving}</> : labels.form.save}</button></div></form></div>}</div>;
}
